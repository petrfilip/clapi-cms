import {React} from "preact";

const Block = (props) => (
    <>
      <div>Block</div>
      <select>
        <option>Gallery</option>
        <option>Text</option>
        <option>Quote</option>
        <option>Image</option>
      </select>
    </>
);

export default Block;
